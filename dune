(include_subdirs unqualified)

(executable
  (name main)
  (flags (:standard -open Base))
  (libraries
    base
    stdio
    mparser
    topological_sort
    zarith)
  (preprocess (pps ppx_sexp_conv)))

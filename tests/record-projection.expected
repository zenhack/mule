
Desugared:

	(. (letrec (types ()) (values ((x {}))) (.= x x {})) x)

inferred type:

	(record (types ()) (values ()))

Runtime term:

	(App
	 ((GetField x)
	  (LetRec
	   (((0 (Record ()))) (Update (old (Record ())) (label x) (field (Var 0)))))))

Evaluated:

	(Record ())


Desugared:

	((. x) (letrec (types ()) (values ((x {}))) (((.= x) {}) x)))

inferred type:

	(all a (record (types (...a)) (values ())))

Runtime term:

	(App
	 ((GetField x)
	  (LetRec
	   (((0 (Record ())))
	    (App
	     ((App
	       ((Lam
	         (0 () (Lam (1 () (Update (old (Var 1)) (label x) (field (Var 0)))))))
	        (Record ())))
	      (Var 0)))))))

Evaluated:

	(Record ())


Desugared:

	(Lam (x (App ((GetField (Strict y)) (Var x)))))

inferred type:

	(Fn
	 (()
	  (Record (r_info ()) (r_types (() () (a)))
	   (r_values (() ((y (Var (() b)))) (c))))
	  (Var (() b))))

Runtime term:

	(Lam (0 () (App ((GetField (Strict y)) (Var 0)))))

Evaluated:

	(Lam (0 () (App ((GetField (Strict y)) (Var 0)))))

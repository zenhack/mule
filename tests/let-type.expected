
Desugared:

	(lettype (t (rec t (record (types ()) (values ()))))
	 ((: t) (record (types ()) (values ()))))

inferred type:

	(record (types ()) (values ()))

Runtime term:

	(Record ())

Evaluated:

	(Record ())


Desugared:

	(: (letrec (types ()) (values ((x {}))) (.= x x {}))
	 (record (types ((t (rec t (record (types ()) (values ()))))))
	  (values ((x (rec t (record (types ()) (values ()))))))))

inferred type:

	(record (types ((t (record (types ()) (values ())))))
	 (values ((x (record (types ()) (values ()))))))

Runtime term:

	(LetRec
	 (((0 (Record ()))) (Update (old (Record ())) (label x) (field (Var 0)))))

Evaluated:

	(Record ((x (Record ()))))

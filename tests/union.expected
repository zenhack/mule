
Desugared:

	((fn x (Ok x)) {})

inferred type:

	(union ((Ok (record (types ()) (values ())))))

Runtime term:

	(App ((Lam (0 () (Ctor (Ok (Var 0))))) (Record ())))

Evaluated:

	(Ctor (Ok (Record ())))

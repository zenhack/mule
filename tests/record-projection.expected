
Desugared:

	(. (record (types ()) (values ((x (record (types ()) (values ())))))) x)

inferred type:

	(record (types ()) (values ()))

Runtime term:

	(App ((GetField x) (LetRec (((0 (Record ()))) (Record ((x (Var 0))))))))

Evaluated:

	(Record ())

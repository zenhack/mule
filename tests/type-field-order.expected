
Desugared:

	(record (types (((t (rec t int))))) (values ((x t (Integer 4)))))

inferred type:

	(record (types ((t int))) (values ((x int))))

Runtime term:

	(LetRec (((0 (Const (Integer 4)))) (Record ((x (Var 0))))))

Evaluated:

	(Record ((x (Const (Integer 4)))))


Desugared:

	(letrec (types ((t (lam a (rec t int))))) (values ()) (.type= t {} t))

inferred type:

	(all a (record (types ((t (lam b int)) ...a)) (values ())))

Runtime term:

	(Record ())

Evaluated:

	(Record ())

(include_subdirs unqualified)

(library
  (name libmule)
  (public_name libmule)
  (flags (:standard -open Base))
  (libraries
    base
    lwt
    lwt.unix
    mparser
    topological_sort
    yojson
    zarith

    cmdliner)
  (preprocess (pps ppx_sexp_conv ppx_let lwt_ppx)))

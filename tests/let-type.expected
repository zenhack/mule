
Desugared:

	(letrec (types (((t (rec t (record (types ()) (values ()))))))) (values ())
	 (: (record (types ()) (values ())) t))

inferred type:

	(record (types ()) (values ()))

Runtime term:

	(Record ())

Evaluated:

	(Record ())

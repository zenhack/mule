
Desugared:

	(letrec (types ())
	 (values ((x (record (types (((t (rec t int))))) (values ())))))
	 (: (Integer 4) (. x t)))

inferred type:

	int

Runtime term:

	(LetRec (((0 (Record ()))) (Const (Integer 4))))

Evaluated:

	(Const (Integer 4))

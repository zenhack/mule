
Desugared:

	(letrec (types ()) (values ((x {}))) (((.= x) {}) x))

inferred type:

	(record (types ()) (values ((x (record (types ()) (values ()))))))

Runtime term:

	(LetRec
	 (((0 (Record ())))
	  (App
	   ((App
	     ((Lam
	       (0 () (Lam (1 () (Update (old (Var 1)) (label x) (field (Var 0)))))))
	      (Record ())))
	    (Var 0)))))

Evaluated:

	(Record ((x (Record ()))))

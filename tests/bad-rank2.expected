
Desugared:

	(letrec (types ())
	 (values
	  ((rank2 (: (fn rank1 (rank1 (Integer 0))) ((all a (a -> a)) -> int)))))
	 (rank2 (fn $6 (Integer 4))))
Type error: Mismatched types: a and int.
a is an `all`-bound type variable. The code must work for *all* types a, not just int.

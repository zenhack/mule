
Desugared:

	(letrec (types (((t (rec t int))))) (values ((x (: (Integer 4) t))))
	 (.type= t (.= x x {}) t))

inferred type:

	(record (types ((t int))) (values ((x int))))

Runtime term:

	(LetRec
	 (((0 (Const (Integer 4))))
	  (Update (old (Record ())) (label x) (field (Var 0)))))

Evaluated:

	(Record ((x (Const (Integer 4)))))


Desugared:

	(letrec (types ())
	 (values
	  ((x
	    (letrec (types ((t (rec t int)))) (values ()) (((.type= t) {}) (type t))))))
	 ((: (. x t)) (Integer 4)))

inferred type:

	int

Runtime term:

	(LetRec
	 (((0
	    (App ((App ((Lam (0 () (Lam (1 () (Var 1))))) (Record ()))) (Record ())))))
	  (Const (Integer 4))))

Evaluated:

	(Const (Integer 4))

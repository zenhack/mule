
Desugared:

	(record (types ()) (values ((x (record (types ()) (values ()))))))

inferred type:

	(record (types ()) (values ((x (record (types ()) (values ()))))))

Runtime term:

	(LetRec (((0 (Record ()))) (Record ((x (Var 0))))))

Evaluated:

	(Record ((x (Record ()))))

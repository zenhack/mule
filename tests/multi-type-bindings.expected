
Desugared:

	(letrec (types ((t (rec t int)) (u (rec u t)))) (values ())
	 (.type= t (.type= u {} u) t))

inferred type:

	(all a (record (types ((t int) (u int) ...a)) (values ())))

Runtime term:

	(Record ())

Evaluated:

	(Record ())

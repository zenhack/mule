
Desugared:

	((fn x (Ok x)) {})

inferred type:

	(all a (all b (union ((Ok (record (types (...a)) (values ()))) ...b))))

Runtime term:

	(App ((Lam (0 () (Ctor (Ok (Var 0))))) (Record ())))

Evaluated:

	(Ctor (Ok (Record ())))


Desugared:

	(letrec (types ((a (rec a int)))) (values ())
	 (letrec (types ((b (rec b a)))) (values ()) (Integer 0)))

inferred type:

	int

Runtime term:

	(Const (Integer 0))

Evaluated:

	(Const (Integer 0))

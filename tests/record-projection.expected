
Desugared:

	((. x) (fix/record (fn $0 (((.= x) {}) {}))))

inferred type:

	(all a (record (types (...a)) (values ())))

Runtime term:

	(App
	 ((GetField (Strict x))
	  (App ((Fix Record) (Lam (0 () (Record ((x (Record ()))))))))))

Evaluated:

	(Record ())


Desugared:

	(record (types (((t (lam a (rec t ((t : t) -> int))))))) (values ()))

inferred type:

	(record (types ((t (lam a (rec b (b -> int)))))) (values ()))

Runtime term:

	(Record ())

Evaluated:

	(Record ())

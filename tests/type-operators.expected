
Desugared:

	(letrec (types ((t (lam a (rec t int))))) (values ())
	 ((: (t (record (types ()) (values ())))) (Integer 4)))

inferred type:

	int

Runtime term:

	(Const (Integer 4))

Evaluated:

	(Const (Integer 4))

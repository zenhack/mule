
Desugared:

	(: (record (types ()) (values ()))
	 (exist $12 (record (types ((t $12))) (values ()))))

inferred type:

	(exist a (record (types ((t a))) (values ())))

Runtime term:

	(Record ())

Evaluated:

	(Record ())

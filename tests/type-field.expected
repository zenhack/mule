
Desugared:

	(letrec (types ((t (rec t int)))) (values ((x (: (Integer 4) t))))
	 (.type= t (((.= x) {}) x) t))

inferred type:

	(all a (record (types ((t int) ...a)) (values ((x int)))))

Runtime term:

	(LetRec
	 (((0 (Const (Integer 4))))
	  (App
	   ((App
	     ((Lam
	       (0 () (Lam (1 () (Update (old (Var 1)) (label x) (field (Var 0)))))))
	      (Record ())))
	    (Var 0)))))

Evaluated:

	(Record ((x (Const (Integer 4)))))

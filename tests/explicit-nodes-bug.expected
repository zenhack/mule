
Desugared:

	(letrec
	 (types
	  (((sig (lam k (rec sig (exist $6 (record (types ((t $6))) (values ())))))))))
	 (values
	  ((from-order
	    (:
	     (fn ord (record (types (((t (lam v (rec t (. ord t))))))) (values ())))
	     (all a ((record (types ((t (rec t a)))) (values ())) -> (sig a)))))))
	 (Integer 0))

inferred type:

	int

Runtime term:

	(LetRec (((0 (Lam (0 () (Record ()))))) (Const (Integer 0))))

Evaluated:

	(Const (Integer 0))


Desugared:

	(letrec (types ((t (rec t int)))) (values ())
	 (letrec (types ((u (rec u t)))) (values ()) (.type= t (.type= u {} u) t)))

inferred type:

	(record (types ((t int) (u int))) (values ()))

Runtime term:

	(Record ())

Evaluated:

	(Record ())

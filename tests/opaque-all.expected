
Desugared:

	(: (fn x {})
	 ((exist $6 (record (types ((t $6))) (values ()))) ->
	  (record (types ()) (values ()))))

inferred type:

	((exist a (record (types ((t a))) (values ()))) ->
	 (record (types ()) (values ())))

Runtime term:

	(Lam (0 () (Record ())))

Evaluated:

	(Lam (0 () (Record ())))


Desugared:

	(fn x ((. y) x))

inferred type:

	(all a (all b (all c ((record (types (...b)) (values ((y c) ...a))) -> c))))

Runtime term:

	(Lam (0 () (App ((GetField (Strict y)) (Var 0)))))

Evaluated:

	(Lam (0 () (App ((GetField (Strict y)) (Var 0)))))

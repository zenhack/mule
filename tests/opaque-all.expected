
Desugared:

	(: (fn x (record (types ()) (values ())))
	 ((exist $12 (record (types ((t $12))) (values ()))) ->
	  (record (types ()) (values ()))))

inferred type:

	((exist a (record (types ((t a))) (values ()))) ->
	 (record (types ()) (values ())))

Runtime term:

	(Lam (0 () (Record ())))

Evaluated:

	(Lam (0 () (Record ())))

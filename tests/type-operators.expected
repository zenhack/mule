
Desugared:

	(let-type ((t ((lam $0 (lam a (rec t $0))) int)))
	 ((: (t (record (types ()) (values ())))) (Integer 4)))

inferred type:

	int

Runtime term:

	(Const (Integer 4))

Evaluated:

	(Const (Integer 4))

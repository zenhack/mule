
Desugared:

	(:
	 (record (types (((t (lam a (rec t (record (types ()) (values ()))))))))
	  (values ()))
	 (exist b (record (types ((t (lam a (rec t b))))) (values ()))))

inferred type:

	(exist a (record (types ((t (lam b a)))) (values ())))

Runtime term:

	(Record ())

Evaluated:

	(Record ())

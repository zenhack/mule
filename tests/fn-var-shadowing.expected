
Desugared:

	(letrec (types ((t (lam a (rec t ((t : t) -> int)))))) (values ())
	 (.type= t {} t))

inferred type:

	(all a (record (types ((t (lam b (rec c (c -> int)))) ...a)) (values ())))

Runtime term:

	(Record ())

Evaluated:

	(Record ())


Desugared:

	(letrec (types (((t (lam a (rec t int)))))) (values ()) (.type= t {} t))

inferred type:

	(record (types ((t (lam a int)))) (values ()))

Runtime term:

	(Record ())

Evaluated:

	(Record ())

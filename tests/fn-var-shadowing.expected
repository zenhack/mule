
Desugared:

	(letrec (types ((t (lam a (rec t ((t : t) -> int)))))) (values ())
	 (.type= t {} t))

inferred type:

	(record (types ((t (lam a (rec b (b -> int)))))) (values ()))

Runtime term:

	(Record ())

Evaluated:

	(Record ())

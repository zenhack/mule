
Desugared:

	(letrec (types ((t (lam a (rec t int))))) (values ())
	 (: (Integer 4) (t (record (types ()) (values ())))))

inferred type:

	int

Runtime term:

	(Const (Integer 4))

Evaluated:

	(Const (Integer 4))

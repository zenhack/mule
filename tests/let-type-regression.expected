
Desugared:

	(let
	 ($3
	  (fix/record
	   (fn $0
	    (let-type ((a ((lam $1 (rec a $1)) int)) (b ((lam $2 (rec b $2)) a)))
	     (((.type= a) (((.type= b) {}) (type b))) (type a))))))
	 (let-type ((b (. $3 b))) (let-type ((a (. $3 a))) (Integer 0))))

inferred type:

	int

Runtime term:

	(App
	 ((Lam (0 () (Const (Integer 0))))
	  (App ((Fix Record) (Lam (0 () (Record ())))))))

Evaluated:

	(Const (Integer 0))
